<div>
    <form onsubmit="false">
        <fieldset>
            @code{tabIndex = 1}
            @foreach{field : formDefinition.fields}
@includeNamed{field.code}
            @code{tabIndex = tabIndex + 1}
            @end{}
        </fieldset>
    </form>
</div>