<div>
    <form onsubmit="false">
        <fieldset>
            @foreach{field : formDefinition.fields}
              @if{!field.isAnnotatedId()}
			    @includeNamed{field.code}
              @end{}
            @end{}
        </fieldset>
    </form>
</div>